export default function EquiposPage() {
  return (
    <main className="min-h-screen flex flex-col items-center justify-center px-6 py-24">
      <div className="text-center max-w-md flex flex-col items-center gap-6">
        <div className="label-premium text-gold/80">EQUIPOS</div>
        <h1 className="font-serif text-4xl">Equipos inscriptos</h1>
        <p className="text-text-secondary text-sm font-light">Grilla de escudos con hover effect sutil.</p>
        <p className="text-text-tertiary text-sm">(Visible cuando abra la inscripción)</p>
      </div>
    </main>
  );
}
